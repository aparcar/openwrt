# kmod-fs-vfat - stub package
name: kmod-fs-vfat
version: "1.0"
release: 1
license: GPL-2.0

source:
  type: none

metadata:
  section: base
  category: Base system
  title: kmod-fs-vfat
  description: kmod-fs-vfat stub package
  virtual: true

dependencies:
  runtime: []
  build: []

build:
  system: none

install:
  files: []
